function deleteModel({ instance, models }, name) {
  if (typeof name !== "string")
    throw new TypeError("Invalid provided name type, expecting string");

  instance.deleteModel(name);
  models.delete(name);
}

module.exports = deleteModel;
